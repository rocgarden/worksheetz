-Create a "My Worksheets" dashboard to list and load these saved worksheets 
-- Allow users to insert their own worksheets
create policy "Allow insert for own user"
on public.worksheets
for insert
with check (auth.uid() = user_id);

-- Allow users to select their own worksheets
create policy "Allow select for own user"
on public.worksheets
for select
using (auth.uid() = user_id);

---fix reading/ss generators to match grammar generatir 

